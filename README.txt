Code has been built using Maven.

To run the code, "mvn spring-boot:run" from esure-spring-boot directory.

To run unit tests, "mvn test" from esure-spring-boot directory.

Testing of url responses has all been completed in browser. URL should be in format http://localhost:8080/cars?colour=red

